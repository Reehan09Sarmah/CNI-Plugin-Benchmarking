# ==========================================
# CNI BENCHMARK PIPELINE CONFIGURATION
# ==========================================

# 1. Name of the test (This prefixes all your CSV files)
# Examples: flannel, calico_vxlan, calico_bgp, cilium_vxlan, cilium_bgp
TEST_NAME="calico_vxlan"

# 2. Node Placement (Where the pods will live)
SERVER_NODE="reehan-pc"
CLIENT_NODE="midnight-07"

# 3. Test Parameters
RUNS=5
THROUGHPUT_TIME=30
LATENCY_TIME=10
FORTIO_TIME="15s"